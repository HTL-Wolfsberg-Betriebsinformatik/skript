# pnpm 11+ reads only `allowBuilds`.
allowBuilds:
  esbuild: true
  playwright-chromium: true

# Fallback for pnpm 10.x, which ignores `allowBuilds`.
onlyBuiltDependencies:
  - esbuild
  - playwright-chromium
